services:
  - type: web
    name: healthcare-deterioration-api
    env: python
    plan: free # or standard, depending on your choice
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
